server {
    listen 80;
    server_name meganalise.pro www.meganalise.pro;
    root /usr/share/nginx/html;
    index index.html;

    include /etc/nginx/mime.types;
    types {
        application/javascript js mjs;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
